{
  "name": "kursor-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd kursor-editor && npm install && npm run build",
    "start": "node server.js",
    "dev": "cd kursor-editor && npm run dev",
    "install-frontend": "cd kursor-editor && npm install"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/material": "^7.3.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "motion": "^12.22.0",
    "react-resizable": "^3.0.5",
    "react-split": "^2.0.14"
  },
  "devDependencies": {
    "@types/react-resizable": "^3.0.8"
  }
}
